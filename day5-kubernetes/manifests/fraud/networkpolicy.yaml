apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: fraud-allow-from-payments-only
  namespace: fraud
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: payments